// Importing necessary libraries and components
import React from "react";
import CustomModal from "../components/CustomModal";

// UsContact component definition
const UsContact = () => {
  return (
    <div>
      {/* Rendering the CustomModal component with specific props */}
      <CustomModal id="1333" type={"B"} />
    </div>
  );
};

// Exporting the UsContact component to be used in other parts of the application
export default UsContact;
